import React from "react";
import { Img } from "./CreatePost";

export default function Media() {
  return (
    <></>
    // <div className='file_container'>
    //   {pics?.map((picFile: any, index: number) => (
    //     <Img
    //       key={index}
    //       index={index}
    //       file={picFile}
    //       onRemove={(rmIndx: any) =>
    //         setPics(pics.filter((pic, index) => index !== rmIndx))
    //       }
    //     />
    //   ))}
    // </div>
  );
}
